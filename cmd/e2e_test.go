package cmd_test

import (
	"testing"
)

func TestTrackAndListEndToEnd(t *testing.T) {
	//_ = exec.Command("../habits", "track", "meditate", "10 minutes").Run()
	//out, err := exec.Command("../habits", "list").Output()
	//if err != nil {
	//	t.Fatalf("List command failed: %v", err)
	//	}
	//	if string(out) == "" {
	//		t.Error("Expected output from list")
	//	}
}
